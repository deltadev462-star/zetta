version: '3.8'

services:
  web:
    build: .
    container_name: zetta-med-platform
    ports:
      - "3020:80"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    networks:
      - app-network

networks:
  app-network:
    driver: bridge